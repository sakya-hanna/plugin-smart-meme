# 智能表情包命中逻辑优化设计方案

分支：develop  
状态：设计阶段，尚未实施  
目标：优先提高内容直接命中率和用户体验；在不牺牲优秀语义设计与命中质量的前提下，减少额外 LLM 调用并优化使用成本

## 1. 当前实现梳理

### 1.1 完整运行链路

```text
用户消息进入 AstrBot
  → AI 生成主回复
  → on_decorating_result / prepare_meme
  → 概率门控
  → 将用户问题和 AI 回复拼接
  → 情绪关键词匹配
  → 内容描述全文匹配
  → 两项都命中：数据库精确查找
  → 任意一项未命中：调用 LLM 分类
  → 校验情绪、内容及真实组合
  → 数据库精确查找启用图片
  → after_message_sent 发送图片
```

### 1.2 概率门控

当前代码在 `main.py` 中先执行 `probability_hit()`。概率未命中时直接结束，不读取候选、不调用 LLM。

优点：

- 成本最低的判断放在最前面；
- 不会因为分类失败影响主文本回复；
- 不会出现概率叠加导致实际概率变成 p²。

注意：概率命中后，当前内容/情绪分类仍可能触发额外 LLM 请求。

### 1.3 情绪匹配

当前 `backend/matching.py::keyword_match()`：

- 将 `question + answer` 拼成一段文本；
- 遍历情绪标签；
- 使用 `text.find(name)` 查找标签第一次出现的位置；
- 返回最早出现的一个情绪。

当前标签库共有 20 个情绪，但只有附着到启用图片的情绪才会成为有效候选。

### 1.4 内容匹配

当前内容匹配同样使用 `text.find(description)`，但 `description` 是完整自然语言句子，例如：

```text
白发猫耳女孩闭眼微笑，用手指触碰脸颊。
```

只有用户问题或 AI 回复中完整出现同样句子时才会命中。普通聊天几乎不会原样复述图片描述，因此内容直接命中率天然很低。

### 1.5 LLM fallback

当情绪或内容任一项没有直接命中时，当前服务：

1. 从启用图片生成真实 `(emotion, content_description)` 组合；
2. 根据已知情绪或内容进行预过滤；
3. 把情绪列表、内容列表和真实组合发送给 LLM；
4. 要求 LLM 返回 JSON；
5. 服务端校验字段白名单和精确组合；
6. 通过数据库精确查找图片。

当前已经防止了“LLM 自由拼接不存在组合”的问题。

### 1.6 图片发送

匹配成功后保存待发送图片；主文本发送完成后，通过 `after_message_sent` 解析安全路径并发送图片。图片发送失败不会替换或阻止主文本。

## 2. 当前方案优点

### 2.1 生命周期隔离较好

- 概率判断先于昂贵分类；
- 图片属于主回复后的次级发送；
- 图片异常不会阻塞主文本；
- 已有插件来源、自身消息和重复处理保护。

### 2.2 数据一致性约束较强

- 内容描述存储在图片记录，而不是全局内容标签；
- 情绪可以多选；
- 只使用启用图片；
- LLM 结果经过允许值校验；
- 精确匹配要求情绪和内容组合同时存在；
- 图片路径有目录 containment 校验。

### 2.3 实现简单且可解释

- 关键词命中结果容易记录和复现；
- 数据库查询是确定性的；
- 随机选图只发生在同一合法组合的候选图片内；
- 失败点已有阶段日志。

### 2.4 安全成本边界清晰

- 概率未命中不调用 LLM；
- 已知一维标签时会预过滤另一维候选；
- 没有真实候选组合时不会调用 LLM；
- LLM 不能选到禁用图片或不存在的组合。

## 3. 当前方案缺点

### 3.1 内容直接命中几乎不可用

完整自然语言描述被当作关键词使用，导致：

```text
用户回复“晚安，做个好梦”
≠ 图片描述“白发角色举着晚安牌子，旁边有星星”
```

即使语义非常接近，也不会命中。

### 3.2 情绪匹配过于简单

- 只使用 `find()`，没有词边界、否定、上下文或同义词处理；
- 标签出现在用户问题或 AI 回复中的顺序会影响结果；
- 多个情绪同时出现时只保留最早出现的一个；
- 新增的细粒度情绪标签可能与普通句子中的词发生误命中；
- 没有情绪强度、置信度和来源记录。

### 3.3 内容和情绪必须同时精确匹配

即使已经可靠命中情绪，但内容只差一个近义词，仍然会进入 LLM fallback，或者最终没有图片。

### 3.4 LLM fallback 输入过大且输出脆弱

- 全量真实组合可能很长；
- 长内容描述之间差异细微，LLM 容易选相近但不合适的组合；
- JSON 格式或组合校验失败时，当前事件直接放弃；
- 同一个回复无法复用分类缓存；
- 当前 LLM fallback 仍是按“标签组合”分类，不是直接在少量图片候选中选择。

### 3.5 缺少本地语义相似度层

当前只有：

```text
全文关键词匹配 → LLM 分类
```

中间缺少不调用 LLM 的本地近似命中层。

### 3.6 数据模型缺少机器可检索字段

目前只有一个面向用户的长内容描述，没有独立保存：

- 图片可见文字；
- 短关键词；
- 同义词/别名；
- 动作词；
- 使用场景词；
- 内容匹配分词结果。

## 4. 参考项目调研结果

参考项目：

```text
/root/astrbot_plugin_meme_manager
远程仓库：https://github.com/anka-afk/astrbot_plugin_meme_manager.git
当前源码：main 分支，提交 3a4cac1，发布版 4.15.4
```

### 4.1 普通情绪路径

参考项目支持模型输出显式情绪标记，例如：

```text
&&开心&&
[开心]
(开心)
```

它的解析器：

- 只接受当前图包中真实存在的标签；
- 支持严格标记、替代标记和重复情绪检测；
- 对松散情绪词进行词跨度判断；
- 用 `max_emotions_per_message` 限制数量；
- 概率只掷一次，并存进 event extra，发送阶段复用。

之后按情绪分类目录本地随机选图，不需要额外内容 LLM。

优点：简单、便宜、快速。  
缺点：准确性依赖模型是否产生正确标记；它不是根据图片内容描述进行匹配。

### 4.2 语义检索路径

参考项目另有离线语义化和 FAISS 检索路径：

1. 对每张图片生成语义 `caption`、细粒度 `tags`、`visible_text`；
2. 将 `caption + tags + visible_text` 构成向量文本；
3. 使用 EmbeddingProvider 建立本地 FAISS `IndexFlatIP`；
4. 查询时只生成短检索词，限制 3–5 个关键词、总长度不超过 30 字；
5. 通过 cosine 相似度、`top_k` 和 `min_score` 得到候选；
6. 将候选限制在当前图包和真实文件内；
7. 再让 LLM 只从候选 ID 中最多选择一张，或留空；
8. 服务端验证候选 ID、entry_id、文件路径和 SHA256。

### 4.3 参考项目的成本问题

语义路径在某些模式下可能包含：

```text
一次短检索词 LLM + 一次候选选择 LLM + 一次 Embedding 请求
```

这对于“减少额外 LLM 调用、降低使用成本”的目标不能直接照搬。

参考项目真正值得复用的不是“多调用模型”，而是：

- 离线准备结构化语义字段；
- 本地候选检索；
- 先缩小候选集；
- 选择阶段只允许候选 ID；
- 严格校验和可观测日志。

## 5. 建议的改进架构

### 5.1 目标链路

```text
主回复生成
  → 一次概率门控
  → 规范化文本
  → 显式/高置信情绪命中
  → 内容可见文字命中
  → 内容别名/短语命中
  → 本地加权相似度候选
  → 情绪与内容候选交集
  → 置信度足够：直接选图，不调用 LLM
  → 置信度不足：只向 LLM 发送 Top-K 候选
  → 服务端校验候选 ID
  → 主文本后发送图片
```

### 5.2 第一层：规范化

对用户问题和 AI 回复分别处理，再合并用于检索：

- Unicode 统一；
- 全角半角统一；
- 中文标点和空白归一化；
- 大小写统一；
- 常见重复字符压缩；
- 去除模型内部标记和控制字段；
- 保留可见文字中的有效短语。

同时保留字段来源：

```text
question_text
answer_text
combined_text
```

避免只依靠拼接顺序造成误判。

### 5.3 第二层：情绪匹配

建立情绪匹配表：

```text
emotion
aliases
negative_patterns
priority
```

第一版不引入运行时 LLM，只做确定性规则：

- 先匹配显式标签或高置信短语；
- 支持用户确认的别名；
- 排除否定语境，例如“不是开心”；
- 保留多个命中情绪及其分数；
- 情绪分数包含来源、位置、词长和置信级别；
- 只保留启用图片实际使用的情绪。

最终情绪不应只返回一个字符串，而应返回：

```json
[
  {"name":"开心","score":1.0,"source":"explicit"},
  {"name":"害羞","score":0.6,"source":"alias"}
]
```

### 5.4 第三层：内容检索字段

在 `images` 表增加机器检索字段，用户展示仍使用原 `content_description`：

```text
visible_text       图片中直接可读的文字
content_keywords   JSON 数组，短关键词/短语
content_aliases    JSON 数组，同义表达/聊天说法
search_text        规范化后的本地检索文本
```

原则：

- `content_description` 负责给人看；
- `visible_text` 保存图片原文字；
- `content_keywords` 和 `content_aliases` 负责本地命中；
- 不把内容关键词建成可复用的全局内容标签；
- 每张图片仍保留独立内容描述。

初始化方式建议分两步：

1. 从现有描述和可读图片文字提取基础短语；
2. 通过 WebUI 允许人工补充/修正别名。

后续上传时生成字段，运行时不调用 LLM 生成关键词。

### 5.5 第四层：本地内容命中

按以下优先级计算内容分数：

```text
visible_text 完整命中       最高分
content_aliases 完整命中    高分
content_keywords 多词命中   中高分
search_text 短语命中         中分
字符/词片段部分命中         低分
```

建议保留参考项目的完整语义候选设计；本地加权规则作为低成本的前置层和候选补充，而不是用来替代语义检索：

```text
exact_visible_text = 1.00
exact_alias        = 0.90
phrase_hit         = 0.75
keyword_coverage   = 0.20–0.70
```

命中结果返回：

```json
{
  "image_id": 123,
  "content_score": 0.90,
  "matched_terms": ["晚安"],
  "source": "alias"
}
```

### 5.6 第五层：情绪-内容候选交集

不要先分别选出情绪和内容再拼接。应直接对启用图片评分：

```text
candidate_score(image)
  = emotion_score(image.emotions)
  + content_score(image)
  + optional context_score
```

处理规则：

- 情绪和内容都有高置信命中：直接从交集中随机选图；
- 只有情绪高置信：从该情绪图片中按内容分数选；
- 只有内容高置信：从该内容图片中按情绪分数选；
- 两者都弱：只取本地 Top-K，进入后续 fallback；
- 没有候选：不调用 LLM，直接记录并结束。

### 5.7 第六层：LLM fallback 限制

只有满足以下条件才调用 LLM：

- 概率已命中；
- 本地检索没有达到直接发送阈值；
- 本地候选数量大于 0；
- 当前事件没有相同文本的分类缓存；
- 未达到每事件/会话成本上限。

LLM 输入不再包含全量图片组合，而只包含 Top-K 候选：

```json
[
  {"id":123,"emotion":["开心"],"description":"...","matched_terms":["晚安"]},
  {"id":456,"emotion":["期待"],"description":"...","matched_terms":["等待"]}
]
```

LLM 只返回：

```json
{"image_id":456}
```

或者：

```json
{"image_id":null}
```

服务端再次验证：

- ID 必须来自本次候选；
- 图片必须启用；
- 文件必须存在；
- 情绪关系必须有效；
- 不接受新标签、新描述或自由组合。

### 5.8 成本与体验平衡

成本优化必须在命中质量和用户体验不下降的前提下进行：

- 优先保留“离线语义化 → 本地候选 → 受限候选选择”的完整设计；
- 通过缓存、请求合并、候选复用和模型选择降低调用成本；
- 本地高置信命中时可以跳过候选选择 LLM，但必须用命中率和误命中率验证；
- 语义候选不足时允许使用必要的 LLM fallback，不以强制 0 调用为目标；
- 候选为空时不调用 LLM；
- 可配置全局 `llm_fallback_enabled`；
- 可配置每天/每会话调用统计和日志。

成本评估同时记录命中率、误命中率、用户体验反馈、延迟和 LLM 调用次数。不能仅因为 Embedding 或候选选择存在额外调用，就否定其对命中质量的价值。

## 6. 分阶段实施建议

### 阶段 A：完整语义基础和本地前置

- 添加规范化文本；
- 添加情绪别名/否定/优先级；
- 为图片增加 `visible_text`、关键词和别名字段；
- 改造本地内容评分；
- 将候选交集和命中分数加入日志；
- 建立图片语义字段和候选数据结构；
- 保留现有 LLM fallback 作为候选选择兜底；
- 同时加入情绪/文字的本地高置信前置命中。

### 阶段 B：候选约束和成本控制

- fallback 只接收本地 Top-K；
- LLM 只返回候选图片 ID；
- 增加事件内分类缓存；
- 增加调用计数和成本日志；
- 增加空候选不调用 LLM 的测试。

### 阶段 C：数据质量和人工优化

- WebUI 编辑图片检索关键词/别名；
- 统计每张图片命中次数；
- 统计关键词命中、直接发送、LLM fallback、最终放弃比例；
- 根据真实日志补充别名，而不是盲目扩大关键词库。

### 阶段 D：语义索引与成本工程

语义索引不是因为成本原因才可选，而应根据用户体验和命中质量决定是否启用：

- 离线 Embedding；
- 本地 FAISS/SQLite 向量索引；
- 本地 Top-K 语义检索；
- 必要时一次候选选择 LLM。

## 7. 主要风险与规避

| 风险 | 规避方式 |
|---|---|
| 关键词扩大导致误命中 | 词长、边界、否定、分数阈值和回归测试 |
| 新情绪标签被普通句子误触发 | 只使用启用图片中的情绪，维护别名和优先级 |
| 内容描述与检索关键词混淆 | 用户描述和机器字段分离 |
| LLM 选择不存在图片 | 只传 Top-K 候选 ID，服务端二次校验 |
| 直接命中后语义变差 | 保留分数、命中词和人工可编辑别名 |
| 规则层过于复杂 | 阶段 A 只使用可解释的加权规则，不先引入向量 |
| 统计口径不一致 | 明确记录每阶段候选数、命中源、LLM 调用次数和最终结果 |

## 8. 成功指标

正式优化前先采集基线，至少统计：

```text
probability_hit_rate
emotion_direct_hit_rate
content_direct_hit_rate
local_intersection_hit_rate
llm_fallback_rate
llm_valid_selection_rate
empty_candidate_rate
image_send_success_rate
```

建议验收目标：

- 内容直接命中率相对基线显著提升；
- LLM fallback 次数/消息下降；
- 本地直接命中发送成功率不下降；
- 非法组合为 0；
- 主文本发送成功率不受影响；
- 图片发送失败仍有明确阶段日志。

具体阈值应在收集当前真实日志基线后确定，避免凭空设置一个无法解释的百分比。
