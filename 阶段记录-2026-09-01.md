# 智能表情包插件阶段记录

记录时间：2026-09-01 17:56:14 CST
阶段状态：页面功能暂阶段测试通过，暂不继续调整页面交互。

## 当前已确认的需求

### 图片与标签模型

- 情绪标签使用全局标签库。
- 一张图片可以选择多个情绪标签。
- 情绪标签在上传和编辑时使用支持搜索的多选下拉框。
- 下拉选项使用可点击标签形式，不使用复选框展示。
- 每张图片只有一个内容描述。
- 内容描述由上传者针对当前图片独立填写。
- 内容描述不进入全局标签库，也不能被其他图片复用。
- 内容描述可以在图库中修改。

### 图库管理

- 支持上传图片、预览图片和搜索图片。
- 支持编辑图片内容描述和情绪标签。
- 每张图片卡片不再显示单独的启用、禁用、删除按钮。
- 图片通过卡片选择框进行多选。
- 图库顶部提供批量操作：
  - 启用选中
  - 禁用选中
  - 删除选中
- 批量操作会根据图片当前状态进行校验：
  - 全部已启用时，启用按钮不可用。
  - 全部已禁用时，禁用按钮不可用。
  - 启用和禁用混合时，启用操作只处理已禁用图片，禁用操作只处理已启用图片。
  - 不符合当前操作状态的图片保持不变。
- 批量删除前会二次确认，并删除数据库记录、标签关联和图片文件。
- 右侧情绪标签库列表有固定最大高度，超出后在列表内部滚动，不再撑大面板。

## 已完成的页面优化

- 图片卡片操作区域已简化。
- 情绪标签改为搜索型多选下拉框。
- 标签选中状态使用高亮样式表示。
- 图库增加批量选择工具栏。
- 批量操作按钮根据选择状态动态启用或禁用。
- 标签库增加内部滚动条。
- 页面保留移动端响应式布局。

## 当前实现位置

插件目录：

`/root/qqbot/data/plugins/astrbot_plugin_smart_meme/`

页面文件：

- `pages/manage/index.html`
- `pages/manage/app.js`
- `pages/manage/style.css`

后端文件：

- `main.py`
- `backend/database.py`
- `backend/service.py`
- `backend/matching.py`

## 当前验证结果

- JavaScript 语法检查通过。
- Python 测试：10 passed。
- Python compileall 检查通过。
- 数据库迁移验证通过。
- 批量数据库操作验证通过。
- 搜索型非复选框情绪标签下拉框冒烟测试通过。
- 批量选择和批量禁用页面冒烟测试通过。
- 混合启用/禁用状态校验逻辑验证通过。
- AstrBot 重启后插件加载成功。
- 用户确认当前页面测试暂时没有问题。

## 后续继续开发时的起点

后续若继续开发，应以本记录为当前页面基线，避免重复修改已经确认没有问题的页面交互。新增功能需要继续遵守：

1. 先确认需求和完整处理链路。
2. 修改后运行现有测试与页面冒烟测试。
3. 涉及批量操作时验证启用、禁用、混合状态和空选择四种情况。
4. 涉及 WebUI 时区分本地页面测试和真实 AstrBot Dashboard 验证。

## 后续待优化：内容命中逻辑

### 现象记录

最近 3 小时日志统计：

- 概率命中：11 次
- 关键词匹配：11 次
- 进入 LLM 分类：7 次
- 成功选择图片：7 次
- 图片发送失败：旧发送路径问题修复前 2 次，修复后暂未发现新的发送失败

新导入图片后，普通 AI 回复大多无法直接命中图片的长内容描述，随后进入 LLM 二次分类。

### 当前原因

当前 `keyword_match()` 使用完整字符串包含匹配：

- 情绪标签：短文本，较容易命中；
- 内容描述：自然语言长句，要求问题或 AI 回复中出现相同连续字符串；
- 普通对话通常不会原样复述内容描述，因此 `content=None` 的比例较高；
- 当前测试配置的触发概率为 100%，进一步放大了 LLM 调用频率。

### 优化方向

后续需要优化内容命中逻辑，在不破坏“每张图片只有一个独立内容描述”的前提下，减少不必要的 LLM 调用。

候选方案：

1. 为每张图片增加独立的本地匹配关键词或别名字段；
2. 保持只有内容描述字段，改为对内容描述进行分词、关键词重叠或短语匹配；
3. 关键词匹配失败后，再根据候选集规模和置信度决定是否调用 LLM；
4. 增加命中率、LLM 调用率和最终发送率日志，便于后续调参。

当前暂不修改命中逻辑，等待后续确认具体方案。
