# 智能表情包命中优化测试方案

分支：develop  
状态：设计阶段，尚未实施  
对应设计：`设计方案-命中优化.md`

## 1. 测试目标

验证优化后同时满足：

1. 内容直接命中率提高；
2. 情绪匹配更稳定，减少误命中；
3. 情绪和内容候选交集合法；
4. 本地可判定时可以减少不必要的 LLM 调用；
5. 必须 fallback 时只发送受约束的候选给 LLM；
6. LLM 不能返回候选集之外的图片；
7. 主文本、图片发送和流式链路不回归；
8. 运行日志能解释每次命中或放弃原因；
9. 不能为了降低调用次数而牺牲命中质量、语义准确性或用户体验。

## 2. 测试状态分类

```text
自动通过：本地 pytest、静态检查、数据迁移测试
容器通过：AstrBot 内编译和启动加载
WebUI 已认证：真实 Dashboard 页面和 API
平台已通过：QQ/NapCat 真实消息
平台未验证：未进行真实 QQ 消息的场景
```

不能用单元测试或插件启动成功替代 QQ/NapCat 验证。

## 3. 基线测试

实施前记录至少 100 条真实或固定回放消息，保存：

```text
question
answer
probability result
keyword result
candidate count
LLM call count
LLM result
selected image
send result
```

基线指标：

```text
B1 情绪直接命中率
B2 内容直接命中率
B3 情绪+内容精确命中率
B4 fallback 调用率
B5 LLM 合法选择率
B6 空候选率
B7 图片发送成功率
B8 平均额外 LLM 调用次数/消息
```

所有优化后的指标必须使用相同消息集和相同概率设置比较。

## 4. 单元测试

### 4.1 文本规范化

| 用例 | 输入 | 期望 |
|---|---|---|
| 全角半角 | `晚安！` / `晚安!` | 规范化后等价 |
| 多空格 | 多个空白和换行 | 规范化后可比较 |
| 大小写 | `MVP` / `mvp` | 英文字母比较不敏感 |
| 重复字符 | `哈哈哈哈` | 按规则压缩或计分，不崩溃 |
| 空文本 | 空问题、空回复 | 返回空匹配 |
| 内部标记 | 插件内部标记 | 不参与内容命中 |

### 4.2 情绪匹配

| 用例 | 期望 |
|---|---|
| 单个显式情绪 | 命中对应标签 |
| 多个情绪 | 返回稳定排序和分数 |
| 否定情绪 | `不是开心` 不应直接按开心高置信命中 |
| 长短标签冲突 | 优先更具体、更长的标签 |
| 别名命中 | 返回标准标签及 alias 来源 |
| 普通词误触发 | 不在合法边界时不命中 |
| 未启用情绪 | 不进入候选 |
| 新增但无图片情绪 | 可在标签库存在，但不参与发送候选 |

### 4.3 内容字段

| 用例 | 期望 |
|---|---|
| 可见文字完整出现 | 最高分直接命中 |
| 可见文字部分出现 | 按覆盖率计分 |
| 别名完整出现 | 高分直接命中 |
| 多个关键词出现 | 根据覆盖率提升分数 |
| 描述全文不出现但语义词出现 | 本地仍可产生候选 |
| 文字大小写差异 | 规范化后命中 |
| 空描述 | 不参与内容候选 |
| 两张图描述相近 | 返回分数排序而非错误拼接 |

### 4.4 情绪-内容交集

| 用例 | 期望 |
|---|---|
| 情绪和内容都高分 | 直接从交集选图，0 次 LLM |
| 只有情绪高分 | 在该情绪图片中按内容分数选 |
| 只有内容高分 | 在该内容图片中按情绪分数选 |
| 两者均低分但有候选 | 生成 Top-K 候选 |
| 没有任何候选 | 不调用 LLM、不发送图片 |
| 候选图被禁用 | 不得选中 |
| 一张图多情绪 | 只要任一匹配情绪满足规则即可进入，但分数可解释 |

### 4.5 LLM fallback

| 用例 | 期望 |
|---|---|
| 本地高置信命中 | `llm_generate` 调用次数为 0 |
| 本地无候选 | 调用次数为 0 |
| 本地 Top-K 候选 | LLM 输入只含 Top-K |
| 返回合法候选 ID | 选择成功 |
| 返回未知 ID | 拒绝并记录 |
| 返回合法图片但不在候选 | 拒绝并记录 |
| 返回自由情绪/内容组合 | 拒绝，不进入数据库查询 |
| 返回空 ID | 不发送图片 |
| 返回 Markdown JSON | 按明确策略解析或拒绝，结果可观测 |
| LLM 异常 | 主文本正常，图片流程安全结束 |
| 超时 | 主文本正常，图片流程安全结束 |
| 同一事件重复调用 | 使用缓存，不重复调用 |

### 4.6 成本计数

- 每条消息只允许一次概率判断；
- 本地命中时额外 LLM 调用数必须为 0；
- fallback 成功或失败都只允许达到配置上限；
- 超过上限后不再调用；
- 日志记录 `llm_call_count`、`local_hit`、`fallback_reason`。

## 5. 数据迁移测试

### 5.1 迁移前快照

保存：

```text
图片数量
启用数量
每张图片 ID、SHA256、描述和情绪关系
```

### 5.2 迁移验证

- 原 `content_description` 不变；
- 新字段能从描述生成；
- 图片文件不变；
- 情绪关系不变；
- 已导入图片的用户可见数据不被重新解释；
- 空描述、特殊字符、重复关键词可安全迁移；
- 迁移可重复执行且结果一致；
- 失败时事务回滚。

## 6. 回放测试集

建立固定 JSONL，每行包含：

```json
{
  "id":"case-001",
  "question":"用户问题",
  "answer":"AI回复",
  "expected_emotions":["开心"],
  "expected_image_ids":[123],
  "expected_local_hit":true,
  "expected_llm_calls":0
}
```

数据集至少覆盖：

- 20 个情绪标签；
- 有图片文字和无图片文字；
- 直接复述文字；
- 近义表达；
- 否定表达；
- 多情绪回复；
- 长回复和短回复；
- 没有适合图片的回复；
- 启用/禁用混合；
- GIF、PNG、JPG；
- 中文、英文、数字和标点。

## 7. 集成测试

### 7.1 正常消息链路

```text
入站消息
→ AI 主回复
→ 概率门控
→ 本地命中/候选生成
→ 必要时一次 LLM fallback
→ 选图
→ 主文本发送
→ 图片发送
```

验证主文本只发送一次，图片最多发送一张。

### 7.2 事件隔离

- 插件自身分类请求不得再次触发图片反应；
- 插件自身发送的图片不得触发新的分类；
- 重复 decorating hook 不得重复发送；
- streaming finish 不得重复发送；
- 普通文本和图片分开发送时顺序正确。

### 7.3 发送失败

构造以下故障：

- 图片文件不存在；
- 图片路径越界；
- `Image.fromFileSystem` 异常；
- NapCat WebSocket 断开；
- 适配器发送异常；
- 发送超时。

期望：

```text
主文本仍正常发送
日志明确标注失败阶段
不会重复发送
不会遗留错误 pending 状态
```

## 8. 性能和成本测试

固定 100 条回放消息，比较优化前后：

```text
总耗时
本地匹配耗时
LLM 调用总数
每消息平均 LLM 调用数
Prompt 字符数
候选图片平均数量
图片发送成功数
```

验收重点：

- 本地命中不产生 LLM 调用；
- fallback prompt 明显小于当前全量组合 prompt；
- 规则层不会因遍历图片造成明显阻塞；
- 主回复延迟不因图片逻辑显著增加。

## 9. WebUI 测试

如果增加关键词/别名字段：

- 创建图片时字段保存正确；
- 修改描述、情绪和关键词为原子操作；
- 清空非法字段有明确错误；
- 刷新后数据仍一致；
- 筛选和吸顶批量操作不受影响；
- 启用/禁用后候选即时变化；
- 禁用图片的标签不进入有效候选。

## 10. 日志验收格式

每条消息至少能还原：

```text
message/event identifier
probability result
emotion matches and scores
content matches and scores
candidate count
local decision
LLM call count
fallback reason
LLM returned ID or validation failure
selected image ID
resolved path
primary text result
secondary image send result
```

敏感信息要求：

- 不记录 API key；
- 不记录完整 prompt 中不必要的个人信息；
- 原始 LLM 返回可截断；
- 日志长度受上限控制。

## 11. 发布前验证命令

```bash
pytest -q
python3 -m compileall -q .
docker exec astrbot sh -lc 'python3 -m compileall -q /AstrBot/data/plugins/astrbot_plugin_smart_meme'
docker logs --since 2m astrbot 2>&1 | grep astrbot_plugin_smart_meme
```

随后必须分别完成：

```text
真实 WebUI 操作
真实 QQ/NapCat 普通消息
真实 QQ/NapCat 直接命中消息
真实 QQ/NapCat fallback 消息
禁用图片后的真实消息
发送失败场景
```

## 12. 验收门槛

改进不能只看“命中数量增加”，必须同时满足：

```text
内容直接命中率提高
额外 LLM 调用率下降
本地命中图片相关性不下降
非法图片/非法组合为 0
主文本不重复、不丢失
图片发送失败可解释
已有测试全部通过
真实 QQ/NapCat 链路通过
```
