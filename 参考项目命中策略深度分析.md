# astrbot_plugin_meme_manager 命中策略深度分析

参考项目：`/root/astrbot_plugin_meme_manager`  
远程仓库：`https://github.com/anka-afk/astrbot_plugin_meme_manager.git`  
研究版本：`main` 分支，提交 `3a4cac1`，发布版 `4.15.4`  
研究方式：读取实际源码、数据模型、语义索引、查询实现和测试，不以 README 推断行为。

## 1. 结论先行

参考项目不是只有一种命中算法，而是两套互斥路径：

```text
路径 A：旧版/兼容情绪标记路径
路径 B：语义检索候选路径
```

两条路径在回复处理时通过 `semantic_mode` 互斥：

```text
semantic_mode == "llm"  → 语义检索 + 情感辅助模型选候选
semantic_mode == "tool" → 让回复模型调用 search_memes 工具
semantic_mode == ""     → 旧版情绪标记处理
```

它的核心原理可以概括为：

```text
不要让模型面对全量图片并自由生成标签。
先把图片离线结构化，再用本地索引缩小候选，
最后只允许模型从候选 ID 中选择。
```

但它的“语义路径”不一定更便宜：在 `llm` 模式下，可能需要短检索词 LLM、Embedding 请求和候选选择 LLM；只有 `tool` 模式和本地索引可用时，才可能把选择动作交给原回复模型完成。

## 2. 命中路径选择机制

### 2.1 请求阶段决定模式

`main.py` 初始化以下语义配置：

```text
semantic.enabled
semantic.vision_provider_id
semantic.embedding_provider_id
semantic.top_k，默认 5
semantic.min_score，默认 0.25
```

请求阶段 `_apply_request_prompt()` 根据配置和运行时图包状态选择模式：

```text
emotion_llm_enabled = true
    → 检查语义包和索引是否就绪
    → 就绪时 semantic_mode = "llm"

emotion_llm_enabled = false
    → 检查回复模型是否支持工具
    → search_memes 工具存在且索引就绪时 semantic_mode = "tool"

其他情况
    → semantic_mode = ""
    → 使用旧版情绪标记路径
```

语义包必须满足：

1. 当前会话有运行时图包；
2. 图包存在语义元数据；
3. 语义元数据完整；
4. 每个可索引图片有 caption 和 tags；
5. Embedding Provider 可用；
6. FAISS 索引存在；
7. Provider ID、模型名和向量维度都与索引清单一致；
8. FAISS 条目数量与元数据一致。

任意条件不满足，参考项目会自动回退旧版分类，不会强行调用不完整的语义路径。

这是一个很重要的“能力门控”：

```text
语义能力不存在 → 旧路径继续工作
语义数据不完整 → 不把半成品作为检索目标
模型/索引不匹配 → 直接禁用语义路径
```

### 2.2 事件来源隔离

项目在 `on_waiting_llm_request` 阶段记录当前请求来源：

```text
chat
plugin
```

之后 `_scope_allows_llm_origin()` 决定是否允许当前结果附加表情。

这避免：

- 插件自己的辅助 LLM 结果再次触发表情；
- 语义候选选择结果重新进入处理链；
- 插件内部请求污染正常聊天请求；
- 多个 hook 重复处理同一个回复。

`_resp_impl()` 还使用：

```text
meme_manager_semantic_response_processed
```

在异步工作开始前占用当前事件，阻止重复 hook 覆盖选择结果。

这部分属于事件生命周期控制，不是图片相似度算法，但对“命中一次、发送一次”非常关键。

## 3. 路径 A：旧版情绪标记命中

### 3.1 标记驱动而非语义推断

旧版路径期望模型在回复中产生情绪标记：

```text
&&开心&&
[开心]
(开心)
```

`_resp_impl()` 按阶段处理：

```text
1. 严格 &&emotion&& 标记
2. 可选 [emotion] 标记
3. 可选 (emotion) 标记
4. 重复情绪检测
5. 宽松情绪词匹配
6. 情绪去重和数量限制
7. 发送阶段按情绪目录取图
```

### 3.2 严格标记解析

严格标记只接受当前运行时图包中的真实情绪名称：

```text
合法 → 记录情绪并从可见文本移除
非法 → 根据配置移除或保留
```

非法标记不会直接拿去查目录，减少路径和标签注入风险。

### 3.3 替代标记

项目支持：

```text
[开心]
(开心)
```

圆括号不会无条件当成情绪标记，而是通过 `_is_likely_emotion_markup()` 判断，避免把普通句子中的括号误当情绪。

### 3.4 重复情绪检测

项目有专门的重复模式识别：

```text
高置信情绪：重复两次即可识别
普通情绪：通常需要重复三次以上
```

并且：

- 跳过 `<think>`、`<thinking>` 等思考区；
- 只对满足长度条件的词处理；
- 从文本移除已识别重复串；
- 将识别结果加入情绪列表。

这是一种“高精度优先”的补充规则，而不是普通全文 `find()`。

### 3.5 宽松情绪词匹配

宽松模式使用：

```text
find_unprotected_word_spans()
_is_likely_emotion()
```

它不是简单 `text.find()`，而是：

1. 找出候选词跨度；
2. 跳过思考标签；
3. 判断该词是不是独立情绪表达，而不是普通单词或句子片段；
4. 只删除确认命中的跨度；
5. 保留命中来源。

这比当前插件直接把 `question + answer` 后使用 `find()` 更能降低误命中。

### 3.6 情绪模型调用时机

旧版路径在宽松匹配之后才考虑情绪 LLM：

```text
前面的显式/重复/宽松规则已经找到有效情绪
    → 仍可按配置决定是否补充模型结果

概率未命中
    → 不调用情绪模型

没有有效标签
    → 模型可返回 0 到 N 个标签
```

模型提示词要求：

```json
{"emotions":["tag1","tag2"]}
```

解析器会：

- 支持 JSON 对象或 JSON 数组；
- 只保留当前图包真实标签；
- 去重；
- 限制最大数量；
- 空结果安全结束。

### 3.7 旧版选图

最终情绪列表确定后：

```text
每个情绪对应一个本地目录
→ 列出该目录图片
→ random.choice()
→ 可选静态图转 GIF
→ 合并发送或 after_message_sent 分开发送
```

这条路径的命中含义是：

```text
命中了一个“情绪类别”
```

而不是：

```text
命中了某张具体图片的内容
```

因此它适合低成本情绪反应，但不适合在大量相似图片中进行精细内容选择。

## 4. 路径 B：语义检索命中

### 4.1 图片离线语义化

每张图片有一份语义记录，核心字段包括：

```text
caption
 tags
 visible_text
 category
 category_description
 category_fit
 category_review_status
 content_sha256
 relative_path
 text_hash
 embedding_status
```

其中：

- `caption`：一到两句自然中文，描述聊天中真正传达的梗义和使用场景；
- `tags`：6 到 10 个细粒度语义标签；
- `visible_text`：图片清晰可见的原始文字；
- `category`：用户图包的主分类；
- `content_sha256`：文件完整性指纹；
- `text_hash`：当前向量输入文本的指纹。

参考项目没有把向量文本简单写成“人物+表情”，而是使用：

```text
图片含义：caption
固定分类标签：category:xxx
分类含义：category_description
语义标签：tags
图片文字：visible_text
```

这相当于为向量检索提供多个语义维度：

```text
聊天潜台词
分类先验
细粒度语义标签
原始文字
```

### 4.2 分类先验不是绝对真理

参考项目将原有分类作为高优先级先验，但允许明确图像证据推翻：

```text
相容 → match
不确定 → uncertain
明显冲突 → conflict
```

如果判定冲突，模型可以建议其他已有分类；后端再移动或进入人工复核。

它特别防止一种错误：

```text
当前分类是 angry
→ 图片只是冒汗、慌张、尴尬笑
→ 不能为了迁就分类硬写成 angry
```

这提升了离线语义元数据质量，间接提升后续命中质量。

### 4.3 Embedding 文本和向量索引

`semantic_index.py` 使用 AstrBot 的 EmbeddingProvider，并做了：

- Provider ID 读取；
- 模型名读取；
- 维度读取；
- 向量归一化；
- 维度校验；
- 查询向量缓存；
- 批量 Embedding；
- FAISS `IndexFlatIP`；
- `IndexIDMap2`；
- 索引清单和向量输入哈希。

由于向量已经 L2 归一化，内积等价于 cosine similarity：

```text
cosine(a,b) = normalized_a · normalized_b
```

搜索时：

1. 生成查询向量；
2. 归一化；
3. 从 FAISS 取 `top_k`；
4. 丢弃低于 `min_score` 的结果；
5. 根据索引清单把 FAISS ID 映射回内容 hash；
6. 从元数据恢复 caption、tags 和路径信息。

### 4.4 索引不是“有文件就算可用”

`index_is_ready()` 会检查：

```text
索引格式
元数据 schema 版本
条目数
向量维度
Provider ID
模型名
FAISS 条目数
语义记录数量
每条 text_hash
索引清单 text_hash
```

只要图片 caption/tags 或向量输入发生变化，索引就可能被判定为不再就绪。

这避免了“图片已替换但仍使用旧向量”的隐蔽错误。

### 4.5 查询词生成

`_build_emotion_semantic_query()` 的作用不是直接选图，而是把机器人可见回复压缩成短查询：

```text
3 到 5 个关键词或短语
总长度不超过 30 个汉字
重点描述聊天用途、情绪、语气、氛围或动作
```

如果情感模型不可用，就使用：

```text
compact_semantic_query(visible_text)
```

因此它有 fallback，不会因短查询 LLM 不可用而完全失效。

但这一步本身可能产生一次额外 LLM 调用，是成本上的缺点。

### 4.6 本地向量候选

`search_memes()` 调用本地 FAISS 检索，并返回最多 `top_k` 个候选。

它会提前拒绝：

```text
空查询
无语义元数据
语义元数据未完成
```

没有候选时直接返回原因：

```text
没有找到足够匹配的表情包
```

它还会移除不应该暴露给模型的字段：

```text
content_sha256
entry_id
score
```

之后再通过 `candidate_records()` 在内部补回完整校验字段。

这是“公开候选”和“内部安全记录”分离的设计。

### 4.7 候选记录和 ID 安全校验

查询结果的公开 ID 类似：

```text
meme:123456789abc
```

但内部会保存：

```text
entry_id
content_sha256
relative_path
caption
tags
```

模型只能看到候选的：

```text
id
caption
tags
```

模型返回 ID 后，`validate_selected_id()` 依次检查：

1. ID 格式是否合法；
2. ID 是否存在于本轮候选缓存；
3. entry_id 是否与公开前缀一致；
4. 元数据记录是否存在；
5. 是否属于人工复核分类；
6. 相对路径是否安全；
7. 图片文件是否存在；
8. 文件 SHA256 是否与元数据一致。

因此模型即使返回了一个看似合法但不在本轮候选的 ID，也不能被发送。

## 5. 语义模式的两种模型交互方式

### 5.1 `tool` 模式：回复模型直接调用工具

请求提示词要求：

```text
本轮必须调用且只能调用一次 search_memes
候选非空时必须选择最贴合的一张
最终文本使用 &&meme:候选ID&&
候选为空时可以不添加表情
不得捏造候选 ID
```

流程：

```text
原回复模型
  → 调用 search_memes
  → 获得 Top-K 候选
  → 原回复模型选择一个候选
  → 在最终文本写入 meme ID 标记
  → 后端清理标记
  → 后端校验 ID
  → 后端发送图片
```

优点：

- 可能只需要原回复模型一次请求中的工具调用链；
- 模型知道完整回复语境；
- 后端不需要再启动第二个独立分类流程。

缺点：

- 强依赖 Provider 的工具调用能力；
- 依赖模型严格遵守工具调用和标记格式；
- 工具调用可能仍然增加原请求 token 和推理成本；
- 如果模型不调用工具，语义模式可能采用默认首候选；
- 需要复杂的候选 ID 与事件缓存安全机制。

### 5.2 `llm` 模式：情感辅助模型二次选择

流程：

```text
AI 回复完成
  → 情感模型生成短检索词
  → Embedding 查询本地 FAISS
  → 获取 Top-K 候选
  → 情感模型只从候选中选一个 ID
  → 后端校验 ID
  → 图片发送
```

候选选择提示词明确要求：

```text
最多选择一张
不适合使用时留空
只能返回候选真实存在的 ID
不要输出图片说明、caption 或 tags
```

优点：

- 原回复模型和选图模型解耦；
- 候选选择逻辑更容易控制；
- 模型不面对全量图片；
- 失败时可以保留主文本。

缺点：

- 可能有两次 LLM 调用；
- 需要 Embedding 请求；
- 选图延迟更高；
- 不能直接满足本项目“尽量少调用额外 LLM”的首要目标。

## 6. 参考项目真正可复用的部分

### 6.1 应复用

```text
1. 语义离线化，而不是运行时临时理解图片。
2. 用户描述和机器检索字段分离。
3. 本地候选优先，LLM 只看 Top-K。
4. 模型只返回候选 ID，不返回自由标签组合。
5. 后端对候选 ID、路径、文件和 hash 二次校验。
6. 语义能力不完整时自动回退，不强行启用。
7. 对情绪标记采用边界、重复和思考区过滤。
8. 记录候选、检索词、模型选择和失败原因。
9. 同一事件设置处理占用标记，防止重复 hook。
10. 把分类质量、索引质量和发送安全分层处理。
```

### 6.2 不应直接复用

```text
1. 不应直接照搬两次 LLM 的语义路径。
2. 不应把分类目录结构直接替代当前每图独立描述模型。
3. 不应让运行时 LLM 自由生成情绪和内容组合。
4. 不应为了使用 FAISS 而立即引入在线 Embedding 成本。
5. 不应把 caption 全文当作精确关键词。
6. 不应把分类先验当成不可推翻的真值。
7. 不应使用“候选第一张”作为所有失败情况的无条件降级。
```

## 7. 对本项目的启发

本项目当前最适合采用参考项目的“思想”，而不是完整架构：

```text
第一层：本地情绪规则和别名
第二层：图片可见文字、关键词、短语和别名命中
第三层：本地加权候选排序
第四层：候选交集达到阈值时直接发送
第五层：只有低置信且存在候选时调用一次 LLM
```

对应关系：

| 参考项目 | 本项目可采用的轻量版本 |
|---|---|
| caption | 保留 content_description 给人看 |
| visible_text | 新增图片可见文字字段 |
| tags | 新增每图机器关键词/别名，不建全局内容标签 |
| FAISS Top-K | 第一版先用本地词项评分 Top-K |
| search_memes 工具 | LLM fallback 只返回候选 image_id |
| 情绪标记解析 | 增加边界、别名、重复和否定识别 |
| 语义选择日志 | 记录本地命中分数和候选来源 |

## 8. 成本结论

参考项目的语义路径减少了“全量候选输入”和“自由组合错误”，但不必然减少调用次数。

本项目的成本最优顺序应是：

```text
概率未命中：0 次额外 LLM
本地情绪+内容高置信：0 次额外 LLM
本地内容候选高置信：0 次额外 LLM
本地候选不足但仍有 Top-K：最多 1 次 LLM
本地完全无候选：0 次额外 LLM
```

只有当轻量本地评分经过真实消息测试后仍然不足，才评估离线 Embedding；即使引入向量索引，也应优先使用：

```text
本地 Embedding 查询 → 本地 Top-K → 直接按分数选图
```

而不是默认再增加一次 LLM 候选选择。

## 9. 最终判断

参考项目最成熟的部分不是某一个关键词函数，而是完整的分层边界：

```text
离线语义质量
≠ 本地候选检索
≠ LLM 候选选择
≠ 文件安全校验
≠ 消息发送
```

它通过分层让每个环节都可独立失败、回退和验证。

对于本项目，最重要的改进方向是：

```text
将当前“长描述全文 find + 全量情绪/内容 LLM 组合选择”
改为
“结构化短检索字段 + 本地加权候选 + 受限的一次性 fallback”。
```

这既能吸收参考项目的核心原理，也能避免为提高命中率而增加更多 LLM 调用。
